/**
 * Layout constants for VibeDeck UI
 */

export const Layout = {
  // Button board grid
  boardColumns: 3,
  buttonMinSize: 100,
  buttonGap: 12,
  buttonBorderRadius: 16,

  // Spacing
  screenPadding: 16,
  sectionGap: 24,

  // Touch targets (accessibility minimum)
  minTouchTarget: 44,
};
